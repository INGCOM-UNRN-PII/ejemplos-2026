# Interdependencia global

En este nivel varias clases dependen de estado compartido global, normalmente mediante campos `static`.

Es comun porque parece simple, pero vuelve dificil razonar sobre el sistema y detectar quien cambio el estado compartido.
